streamlit>=1.30,<2.0
pypdf>=4.0,<6.0
scikit-learn>=1.3,<2.0
pandas>=2.0,<3.0
requests>=2.31,<3.0

# Optional but recommended — enables the "Confidential Business Information"
# NER layer. The app runs fine without it (falls back to keyword-only
# detection), so this is not a hard requirement.
spacy>=3.7,<4.0
